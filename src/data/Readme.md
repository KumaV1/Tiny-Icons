`testData.json` adds data for testing. For example, a prayer with conditional modifiers. Or a new item and corresponding synergy, to allow testing the "find index attribute" behaviour.

Modifications corresponding to that test data will also be placed in this file.